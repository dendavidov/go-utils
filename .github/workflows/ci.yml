name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true
      - uses: golangci/golangci-lint-action@v9
        with:
          version: v2.11

  test:
    strategy:
      fail-fast: false
      matrix:
        go-version: ["1.25.x", "1.26.x"]
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v6
        with:
          go-version: ${{ matrix.go-version }}
          cache: true
      - name: Test
        env:
          # go.mod pins toolchain go1.26.x; without this, Go 1.25 jobs download
          # that toolchain and the compiler/stdlib versions disagree.
          GOTOOLCHAIN: ${{ matrix.go-version == '1.25.x' && 'local' || 'auto' }}
        run: go test -race -coverprofile=cover.out ./...
      - name: Upload coverage
        if: runner.os == 'Linux' && matrix.go-version == '1.26.x'
        uses: actions/upload-artifact@v7
        with:
          name: coverage
          path: cover.out
